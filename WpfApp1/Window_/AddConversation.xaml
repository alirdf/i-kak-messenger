<Window x:Class="WpfApp1.Window_.AddConversation"
      
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.Window_"
        mc:Ignorable="d"
        Title="Создать беседу" Height="450" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="70" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
        <TextBlock Text="Название беседы" Margin="10" />
        <TextBox MinWidth="100" x:Name="txtConversationName" Margin="10" Grid.Row="1" />
            
        </StackPanel>

        <TextBlock Text="Участники" Margin="10" Grid.Row="1" VerticalAlignment="Center"/>
        <!--<ListBox x:Name="lbParticipants" Margin="10" Grid.Row="3" SelectionMode="Multiple" />-->
        <ListBox x:Name="lbParticipants" Margin="10" Grid.Row="3" SelectionMode="Multiple" ItemsSource="{Binding Path=Users}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Username}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel  Orientation="Horizontal" HorizontalAlignment="Right" Margin="10" Grid.Row="1">
            <Button Foreground="White" Content="Отмена" Margin="5" Click="CancelButton_Click" />
            <Button Foreground="White" Content="Создать" Margin="5" Click="CreateButton_Click" />
        </StackPanel>
    </Grid>
</Window>
